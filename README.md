# Projeto Piloto - Microfrontend Angular

## Estrutura
- /host → aplicação shell
- /microfrontend → aplicação remota

## Subir os projetos
1. cd microfrontend
   npm install
   ng serve

2. cd host
   npm install
   ng serve
